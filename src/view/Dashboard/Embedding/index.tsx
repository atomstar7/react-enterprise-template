import React from 'react';
import './index.less';

const Embedding = () => {
    return (
        <div className='embedding-root'>
            <div className='embedding-title'>Embedding</div>
            <div className='embedding-body'></div>
        </div>
    );
};

export default Embedding;
